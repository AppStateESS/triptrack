'use strict'
import React, {useState} from 'react'
import PropTypes from 'prop-types'

const Form = ({send}) => {
  return <div>Form</div>
}

Form.propTypes = {
  send: PropTypes.func,
}

export default Form
